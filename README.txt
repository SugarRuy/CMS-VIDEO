本系统使用原生php+mysql开发，使用Win7下Xampp作为部署环境，chrome浏览器作为运行环境。

使用时将所有文件复制在xampp/htdocs/cms 文件夹下(先创建cms文件夹)

将cms.sql 使用导入语句导入数据库。

upload文件夹内是用户上传的文件，已清空。

使用时，访问localhost/cms/user进入用户界面，账户密码user1，user1

访问localhost/cms/admin进入管理页面，账户密码(admin, linux) ; 或(root, admin)
